import React, { Component } from 'react'
import {
  Person
} from '@material-ui/icons'

const User = (props) => {

  return(
    <div className="user-portrait">
      <Person />
    </div>
  )
}

export default User
